import { Component, OnInit } from '@angular/core';

@Component({
  selector: 'app-admins', // Selector del componente
  templateUrl: './admins.component.html', // Ruta de la plantilla HTML del componente
  styleUrls: ['./admins.component.css'] // Rutas de los archivos de estilos CSS del componente
})
export class AdminsComponent implements OnInit {

  constructor() { }

  ngOnInit() {
    // Código que se ejecuta al inicializar el componente
  }

}
