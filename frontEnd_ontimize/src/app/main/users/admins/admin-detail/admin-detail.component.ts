import { Component, OnInit } from '@angular/core';

@Component({
  selector: 'app-admin-detail', // Selector del componente
  templateUrl: './admin-detail.component.html', // Ruta de la plantilla HTML del componente
  styleUrls: ['./admin-detail.component.css'] // Rutas de los archivos de estilos CSS del componente
})
export class AdminDetailComponent implements OnInit {

  constructor() { }

  ngOnInit() {
    // Código que se ejecuta al inicializar el componente
  }

}
